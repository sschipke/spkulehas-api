<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>Past Reservations Have Been Deleted</title>
    <style>
      body { display: flex; align-items: center; justify-content: center;
      font-family: "Roboto","Helvetica","Arial",sans-serif; .ii a[href] { color:
      #FFFFFF; }; }
    </style>
  </head>
  <body>
    <h2>Bulk Reservation Deletion</h2>
    <p>
      {{date}},
        <strong>{{numberOfReservations}}</strong> reservations have been deleted from the 
      <strong>{{environment}}</strong>
      api.
    </p>
    <ul>
    {{#deletedReservations}}
      <li>{{title}}
        <ul>
          <li><strong>id:</strong> {{id}}</li>
          <li><strong>start:</strong> {{start}}</li>
          <li><strong>end:</strong> {{end}}</li>
          {{#if notes}}
            <li><strong>notes: </strong> {{notes}}</li>
          {{/if}}
        </ul>
      </li>
    {{/deletedReservations}}
    </ul>
    {{^deletedReservations}}<p><strong>NO</strong> reservations were deleted.</p>{{/deletedReservations}}
  </body>
</html>
